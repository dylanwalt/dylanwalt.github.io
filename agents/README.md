# Agents

This folder holds “agent profiles” (prompts + conventions) used to work on any site in `sites/<site>/`.

- `website-scraper/`: standard outputs go to `sites/<site>/scraped/`
- `website-builder/`: standard outputs go to `sites/<site>/build/`

