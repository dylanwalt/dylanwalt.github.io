# Port443 — V3

Third iteration of the Port443 rebuild. Produced under the
`website-rebuild-pro` skill's new top-15 industry-research gate and the Apple
design-language reference, both committed in
`.codex/skills/website-rebuild-pro/references/`.

- See `notes/industry-inspiration.md` for the 15 audited sector references
  and the three ★ anchors.
- See `notes/design-plan.md` for how each plan decision cites the brief.
- See `build/README.md` for local run and export instructions.

Public route: `/port443-v3/`. Earlier rebuilds live at `/port443/` and
`/port443-v2/`.
