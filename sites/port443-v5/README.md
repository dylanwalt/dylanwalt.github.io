# Port443 — V5

Fifth iteration of the Port443 rebuild. It keeps the product-led editorial
architecture and pushes the site toward a calmer, more premium control-room
feel:

- Accent colour stays green (`#22c55e`) for continuity with the wordmark.
- Hero becomes a split control-room layout with a stronger product claim.
- Proof rail shifts to evidence-led statements instead of generic tiles.
- The Loop is re-narrated as Observe → Remediate → Verify.
- OneView expands into a larger product band.
- A regional photo band adds breathing room and context.
- A closing CTA band anchors the conversion path.
- New shield + padlock + circuit-trace logo (white and color SVGs).

- See `notes/industry-inspiration.md` for the 15 audited sector references
  and the three ★ anchors.
- See `notes/design-plan.md` for how each plan decision cites the brief.
- See `build/README.md` for local run and export instructions.

Public route: `/port443-v5/`. Earlier rebuilds live at `/port443/`,
`/port443-v2/`, and `/port443-v3/`.
