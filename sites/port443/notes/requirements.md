# Requirements

- Rebuild `https://www.port443.co.za/` as a modern local Next.js site.
- Use the `website-rebuild-pro` skill workflow (UI/UX Pro Max + 21st.dev hero + Framer Motion).
- Start from a hero section aligned to the Port443 brand (cybersecurity, enterprise, MEA).
- Must run locally on `http://localhost:3000`.
