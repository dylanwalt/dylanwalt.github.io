# lawnpro

Per-site rebuild workspace.

- Configure source targets in `site.config.json`
- Keep research, requirements, and design decisions in `notes/`
- Store scraper output under `scraped/`
- Build the local website under `build/`
