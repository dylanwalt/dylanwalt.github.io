# Elm Interiors rebuild

Standalone Elm Interiors source rebuild for the multi-site workspace.

Run locally:

```bash
npm run dev -- --port 3002
```

Static Pages export uses `NEXT_PUBLIC_BASE_PATH=/elm-interiors npm run build`.
