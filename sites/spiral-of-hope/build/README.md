# Spiral of Hope rebuild

Standalone Spiral of Hope source rebuild for the multi-site workspace.

Run locally:

```bash
npm run dev -- --port 3004
```

Static Pages export uses `NEXT_PUBLIC_BASE_PATH=/spiral-of-hope npm run build`.
