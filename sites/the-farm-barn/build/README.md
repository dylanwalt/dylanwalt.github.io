# The Farm Barn rebuild

Standalone The Farm Barn source rebuild for the multi-site workspace.

Run locally:

```bash
npm run dev -- --port 3003
```

Static Pages export uses `NEXT_PUBLIC_BASE_PATH=/the-farm-barn npm run build`.
